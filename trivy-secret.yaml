# Trivy loads this file automatically. Built-in secret rules remain enabled.
# Add a narrow allow-rule only after proving a false positive; every exception
# must have a unique id and a specific description. Do not skip whole source
# trees, example configuration files, or Git history.
rules: []
allow-rules: []
